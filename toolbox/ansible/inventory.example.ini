# Copy to inventory.ini and edit. One host per line; groups in [brackets].
# Connection assumes SSH with key auth and a sudo-capable user.

[linux]
web01 ansible_host=10.0.1.11
web02 ansible_host=10.0.1.12
db01  ansible_host=10.0.1.20

[all:vars]
ansible_user=deploy
# ansible_python_interpreter=/usr/bin/python3
